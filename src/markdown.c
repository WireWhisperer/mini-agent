#include "markdown.h"

void render_markdown(WINDOW *win, const char *markdown)
{
    // int col = 0;
    // int row = 0;

    // char buffer[1024];  //缓存每一行

    // //读取窗口的大小
    // getmaxyx(win, row, col);

    // for (int i = 0; i < row; i++)
    // {
        
    // }

}